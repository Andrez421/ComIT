let notas: number[];
notas = [6, 8, 7.5];

const notas2: Array<number> = [8, 4, 8];

class Vehiculo {
    marca: string;
    modelo: string;
    año: number;
    color: string;

    constructor(marca: string, modelo: string, año: number, color: string) {
    this.marca = marca;
    this.modelo = modelo;
    this.año = año;
    this.color = color;
}
    }
    let vehiculo1 = new Vehiculo('Toyota', 'Rav4', 2024, 'Negro');
    let vehiculo2 = new Vehiculo('Toyota', '4Runner', 2010, 'Gris');
    let vehiculo3 = new Vehiculo('Honda', 'Pilot', 2020, 'Blanco');
    let vehiculo4 = new Vehiculo('Honda', 'CR-V', 2015, 'Rojo');

    console.log(vehiculo1)
    console.log(vehiculo2)
    console.log(vehiculo3)
    console.log(vehiculo4)

    export default {};
    
