function mostrarValor() {
    console.log(valor); 
    let valor = 5;
}
mostrarValor();

// El código intentará acceder a la variable `valor` antes de que haya sido declarada, lo que resultará en un error de referencia
