
const numbers = [1, 2, 3, 4, 5];
const doubledNumbersArrow = numbers.map(number => number * 2);
console.log(doubledNumbersArrow); // [2, 4, 6, 8, 10]
//en un iterador utilizaria arrow function para hacerlo mas corto, rapido y no tener que usar el this
// Las arrow functions son especialmente útiles en iteradores