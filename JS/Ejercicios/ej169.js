  function obtenerFuncion() {

    const nombre = "JavaScript";
  
    // Definimos una función llamada 'mostrarMensaje'
    function mostrarMensaje() {
        // Esta función imprime en la consola el mensaje "Yo <3 JavaScript"
        console.log("Yo <3 " + nombre);
    }
  
    // Llamamos a la función 'mostrarMensaje' para que se ejecute
    mostrarMensaje();
}

// Llamamos a la función 'obtenerFuncion' para que se ejecute todo el código anterior
obtenerFuncion();